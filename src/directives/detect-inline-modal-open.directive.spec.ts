import { DetectInlineModalOpenDirective } from './detect-inline-modal-open.directive';

describe('DetectInlineModalOpenDirective', () => {
  it('should create an instance', () => {
    const directive = new DetectInlineModalOpenDirective();
    expect(directive).toBeTruthy();
  });
});
